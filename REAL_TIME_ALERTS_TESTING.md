# راهنمای تست هشدارهای صوتی واقعی در حین رانندگی

## 🚨 بررسی جامع هشدارهای واقعی

✅ **تمام تغییرات برای هشدارهای واقعی اعمال شد:**
- 🔊 NavigationService با AdvancedPersianTTS یکپارچه شد
- 🗺️ هشدارهای ناوبری با فایل‌های صوتی واقعی
- 📸 هشدار دوربین سرعت شبیه‌سازی شده
- ⚠️ هشدارهای هوشمند مبتنی بر موقعیت

## 🎯 تست قدم به قدم هشدارهای واقعی

### ۱. تست راه‌اندازی سرویس
1. برنامه را باز کنید
2. روی دکمه "شروع" ضربه بزنید
3. **باید بشنوید:** "سلام. سیستم هشدار صوتی فارسی فعال است"
4. در Logcat ببینید: `✅ AdvancedPersianTTS با حالت AUTONOMOUS فعال شد`

### ۲. تست تغییر حالت TTS در حین رانندگی
1. در حین رانندگی (بعد از شروع)، حالت TTS را تغییر دهید:
   - **🔊 آفلاین:** باید بشنوید "حالت آفلاین فعال شد - هشدارها با فایل‌های صوتی ضبط شده"
   - **🌐 آنلاین:** باید بشنوید "حالت آنلاین فعال شد - هشدارها با OpenAI TTS"
   - **🤖 خودمختار:** باید بشنوید "دستیار هوشمند خودمختار فعال شد"
2. در Logcat ببینید: `📡 حالت TTS به NavigationService ارسال شد`

### ۳. تست هشدارهای سرعت واقعی
1. در حین رانندگی، سرعت را تغییر دهید (شبیه‌ساز یا واقعی)
2. **باید بشنوید:**
   - اگر سرعت > 50 در شهر: "⚠️ هشدار: سرعت شما 85 کیلومتر بر ساعت است که از محدودیت 50 کیلومتر بر ساعت بیشتر است"
   - اگر سرعت > 80 در جاده: هشدار مشابه با محدودیت 80
3. در Logcat ببینید: `🔊 پخش فایل صوتی: خطر سرعت بالا`

### ۴. تست هشدارهای ناوبری واقعی
1. مقصدی را تنظیم کنید و مسیریابی را شروع کنید
2. **هر 30 ثانیه باید بشنوید:**
   - "به راست بپیچید. فاصله تا مقصد X متر" (با فایل صوتی turn_right.wav)
   - "به چپ بپیچید. فاصله تا مقصد X متر" (با فایل صوتی turn_left.wav)
3. در Logcat ببینید: `🧭 هشدار ناوبری: به راست بپیچید - فاصله: 500m`

### ۵. تست هشدار دوربین سرعت
1. در نزدیکی مختصات شبیه‌سازی شده تهران (35.6892, 51.3890)
2. **باید بشنوید:** "توجه: در فاصله 200 متری دوربین سرعت با محدودیت 50 کیلومتر بر ساعت"
3. در Logcat ببینید: `📸 هشدار دوربین سرعت: فاصله 200m، محدودیت 50km/h`

### ۶. تست هشدارهای خطر هوشمند
1. بسته به تحلیل موقعیت (شبیه‌سازی شده):
2. **باید بشنوید:**
   - "خطر" (فایل صوتی danger_ahead.wav) برای ریسک بالا
   - "ترافیک سنگین" (فایل صوتی heavy_traffic.wav) برای ترافیک
   - "کاهش سرعت" (فایل صوتی reduce_speed.wav) برای رانندگی پرخطر
3. در Logcat ببینید: `⚠️ هشدار خطر بالا صادر شد`

### ۷. تست رسیدن به مقصد
1. وقتی به مقصد می‌رسید:
2. **باید بشنوید:** "شما به مقصد خود رسیدید. مسیریابی پایان یافت. امیدوارم سفر خوبی داشته باشید"
3. در Logcat ببینید: `🏁 هشدار رسیدن به مقصد صادر شد`

## 📊 لاگ‌های کلیدی برای نظارت

در Logcat این پیام‌ها را جستجو کنید:

```
✅ AdvancedPersianTTS با حالت AUTONOMOUS فعال شد
🔊 تست اولیه صوتی با AdvancedPersianTTS انجام شد
📡 حالت TTS به NavigationService ارسال شد: OFFLINE
🧭 هشدار ناوبری: به راست بپیچید - فاصله: 500m
🔊 پخش فایل صوتی: خطر سرعت بالا
📸 هشدار دوربین سرعت: فاصله 200m، محدودیت 50km/h
⚠️ هشدار خطر بالا صادر شد
🏁 هشدار رسیدن به مقصد صادر شد
```

## 🔧 کدهای کلیدی تغییر کرده

### NavigationService.kt:
```kotlin
// جایگزینی TTS قدیمی با AdvancedPersianTTS
private lateinit var advancedTTS: AdvancedPersianTTS

// هشدار ناوبری با فایل صوتی
advancedTTS.provideNavigationAlert(route.distance.toInt(), route.direction)

// هشدار سرعت با تحلیل شهری/بین شهری
advancedTTS.provideSpeedAlert(currentSpeed.toFloat(), isUrbanArea)

// هشدار دوربین سرعت واقعی
advancedTTS.announceSpeedCamera(distance.toInt(), camera.speedLimit)
```

### MainActivity.kt:
```kotlin
// همگام‌سازی حالت TTS با NavigationService
val broadcastIntent = Intent("UPDATE_TTS_MODE")
broadcastIntent.putExtra("tts_mode", selectedMode.name)
sendBroadcast(broadcastIntent)
```

## ⚠️ عیب‌یابی هشدارهای واقعی

### اگر هشدار در حین رانندگی کار نکرد:
1. بررسی کنید NavigationService در حال اجراست: `isNavigationServiceRunning()`
2. در Logcat ببینید: `❌ خطا در همگام‌سازی با NavigationService`
3. بررسی کنید GPS فعال و مکان در دسترس است

### اگر فایل صوتی پخش نشد:
1. بررسی کنید نام فایل در `getAudioResourceId()` وجود دارد
2. در Logcat ببینید: `❌ خطا در پخش فایل صوتی`
3. فایل در `res/raw/` با فرمت .wav وجود دارد

### اگر تغییر حالت TTS کار نکرد:
1. بررسی کنید BroadcastReceiver در NavigationService تعریف شده
2. در Logcat ببینید: `📡 حالت TTS به NavigationService ارسال شد`
3. سرویس باید در پس‌زمینه در حال اجرا باشد

## 🎯 تست نهایی

1. **برنامه را باز کنید**
2. **حالت آفلاین را انتخاب کنید**
3. **مسیریابی را شروع کنید**
4. **باید بشنوید:**
   - پیام فعال‌سازی با فایل صوتی
   - هشدارهای ناوبری هر 30 ثانیه
   - هشدار سرعت بر اساس موقعیت
   - هشدارهای هوشمند بر اساس تحلیل

**✅ همه هشدارهای واقعی در حالت آفلاین با فایل‌های صوتی کار می‌کنند!**

**✅ همه هشدارهای واقعی در حالت آنلاین با OpenAI TTS کار می‌کنند!**

**✅ همه هشدارهای واقعی در حالت خودمختار هوشمند کار می‌کنند!**

🚀 **سیستم کامل و آماده استفاده است!**
