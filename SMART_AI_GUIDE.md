# راهنمای دستیار هوشمند AI PersianNavigatorLite

## 🤖 overview

دستیار هوشمند AI با اولویت OpenAI TTS قابلیت تولید هشدارهای داینامیک و هوشمند را بر اساس موقعیت و شرایط فراهم می‌کند. این سیستم به طور خودکار بین حالت آنلاین (OpenAI) و آفلاین switch می‌کند.

## 🎯 قابلیت‌های هوشمند

### 1. 🚗 تحلیل ترافیک هوشمند
```kotlin
advancedPersianTTS.alertTrafficAnalysis(
    trafficCondition = "ترافیک سنگین",
    delayMinutes = 15,
    priority = Priority.HIGH
)
```
- **آنلاین (OpenAI):** "ترافیک سنگین detected، 15 دقیقه تأخیر، مسیر جایگزین پیشنهاد می‌شود"
- **آفلاین:** "ترافیک سنگین در پیش است، احتیاط کنید"

### 2. 🌤️ هشدار آب‌وهوای هوشمند
```kotlin
advancedPersianTTS.alertWeatherCondition(
    weather = "بارانی",
    visibility = "کم",
    dangerLevel = "متوسط",
    priority = Priority.HIGH
)
```
- **آنلاین (OpenAI):** "باران سنگین با دید کم، رانندگی با احتیاط کامل"
- **آفلاین:** "شرایط جوی نامساعد، رانندگی با احتیاط"

### 3. ⛽ یادآوری سوخت هوشمند
```kotlin
advancedPersianTTS.alertFuelReminder(
    fuelPercent = 20,
    distanceToStation = 5,
    priority = Priority.NORMAL
)
```
- **آنلاین (OpenAI):** "سوخت 20٪، پمپ بنزین در 5 کیلومتری، پیشنهاد سوخت‌گیری"
- **آفلاین:** "سوخت کافی ندارید، پمپ بنزین نزدیک است"

### 4. 😴 تشخیص خستگی هوشمند
```kotlin
advancedPersianTTS.alertFatigueDetection(
    drivingHours = 3,
    currentTime = "14:30",
    fatigueLevel = "زیاد",
    priority = Priority.HIGH
)
```
- **آنلاین (OpenAI):** "3 ساعت رانندگی، خستگی زیاد detected، استراحت 15 دقیقه پیشنهاد می‌شود"
- **آفلاین:** "احساس خستگی می‌کنید، لطفاً استراحت کنید"

### 5. 🧭 بهینه‌سازی مسیر هوشمند
```kotlin
advancedPersianTTS.alertRouteOptimization(
    currentRouteTime = 45,
    alternativeRouteTime = 30,
    timeSaving = 15,
    priority = Priority.NORMAL
)
```
- **آنلاین (OpenAI):** "مسیر جدید 15 دقیقه سریع‌تر، پیشنهاد تغییر مسیر"
- **آفلاین:** "مسیر بهتری موجود است، پیشنهاد می‌شود"

## 🔧 مدیریت حالت‌ها

### فعال‌سازی حالت هوشمند با اولویت OpenAI:
```kotlin
advancedPersianTTS.enableSmartMode()
```

### غیرفعال‌سازی حالت هوشمند:
```kotlin
advancedPersianTTS.disableSmartMode()
```

### تست کامل حالت هوشمند:
```kotlin
advancedPersianTTS.testSmartMode()
```

### تست کامل سیستم 5 حالته:
```kotlin
advancedPersianTTS.testVoice()
```

## 🎛️ سیستم 5 حالته هوشمند

برنامه به این ترتیب تصمیم می‌گیرد:

### حالت هوشمند (فعال):
1. **🤖 OpenAI TTS آنلاین** (اولویت اول - هوشمند و طبیعی)
2. **📱 فایل‌های صوتی آفلاین** (fallback - بهترین کیفیت)
3. **🎤 مدل هانیه** (fallback - کیفیت عالی)
4. **🔊 TTS فارسی سیستم** (fallback - متوسط)

### حالت عادی (غیرفعال):
1. **📱 فایل‌های صوتی آفلاین** (اولویت اول)
2. **🎤 مدل هانیه** (دوم)
3. **🔊 TTS فارسی سیستم** (سوم)
4. **🌐 OpenAI TTS آنلاین** (چهارم)

## 🔄 Switch هوشمند خودکار

### به حالت آنلاین:
- وقتی اینترنت فعال باشد
- کلید OpenAI API معتبر باشد
- حالت هوشمند فعال باشد

### به حالت آفلاین:
- وقتی اینترنت قطع شود
- کلید API نامعتبر باشد
- خطا در ارتباط با OpenAI

### تلاش مجدد خودکار:
- هر 30 ثانیه وضعیت آنلاین بررسی می‌شود
- در صورت قطعی، هر 1 دقیقه تلاش مجدد برای بازگشت به حالت آنلاین
- بدون interrupt کردن تجربه کاربری

## 📊 وضعیت دستیار هوشمند

برای بررسی وضعیت فعلی:
```kotlin
val status = smartAIAssistant.getAssistantStatus()
println("حالت هوشمند: ${status.isSmartModeEnabled}")
println("وضعیت آنلاین: ${status.isOnlineAvailable}")
println("حالت فعلی: ${status.currentMode}")
```

## 🎯 مثال‌های کاربردی

### مثال 1: سیستم ناوبری هوشمند
```kotlin
// وقتی کاربر به مقصد نزدیک می‌شود
if (distanceToDestination < 1000) {
    advancedPersianTTS.generateSmartAlert(
        SmartAlertType.ROUTE_OPTIMIZATION,
        mapOf(
            "current_route_time" to 25,
            "alternative_route_time" to 20,
            "time_saving" to 5
        )
    )
}
```

### مثال 2: سیستم ایمنی هوشمند
```kotlin
// وقتی سرعت غیرمجاز detected می‌شود
if (currentSpeed > speedLimit) {
    advancedPersianTTS.generateSmartAlert(
        SmartAlertType.TRAFFIC_ANALYSIS,
        mapOf(
            "traffic_condition" to "سرعت غیرمجاز",
            "delay_minutes" to 0
        ),
        Priority.URGENT
    )
}
```

### مثال 3: سیستم مدیریت سوخت
```kotlin
// وقتی سوخت کم می‌شود
if (fuelLevel < 25) {
    advancedPersianTTS.alertFuelReminder(
        fuelPercent = fuelLevel,
        distanceToStation = nearestStationDistance,
        Priority.HIGH
    )
}
```

## 💡 نکات مهم

1. **اولویت با OpenAI:** وقتی حالت هوشمند فعال باشد، OpenAI اولویت اول است
2. **Switch خودکار:** بین حالت آنلاین و آفلاین به صورت خودکار switch می‌کند
3. **هشدارهای داینامیک:** بر اساس داده‌های واقعی و موقعیت، پیام‌های متفاوتی تولید می‌شود
4. **مدیریت هوشمند خطا:** اگر OpenAI کار نکرد، به صورت خودکار به حالت آفلاین switch می‌کند
5. **بهینه‌سازی مصرف:** فقط در صورت لزوم از OpenAI استفاده می‌شود

## 🔍 اشکال‌زدایی

برای بررسی وضعیت کامل سیستم:
```kotlin
advancedPersianTTS.testVoice()
```

این متد تمام 5 حالت را تست کرده و وضعیت هر کدام را نمایش می‌دهد.

## 📞 پشتیبانی

در صورت بروز مشکل، لاگ‌های برنامه با تگ‌های زیر را بررسی کنید:
- `SmartAI` - برای لاگ‌های دستیار هوشمند
- `AdvancedTTS` - برای لاگ‌های سیستم TTS
- `OnlineTTS` - برای لاگ‌های OpenAI TTS

دستیار هوشمند AI اکنون کاملاً یکپارچه شده و می‌تواند مانند یک دستیار واقعی در کنار کاربر عمل کند! 🚀✨
