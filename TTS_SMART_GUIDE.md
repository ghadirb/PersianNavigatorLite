# راهنمای سیستم صوتی هوشمند PersianNavigatorLite

## 🎯 overview

سیستم صوتی هوشمند برنامه با 4 حالت مختلف کار می‌کند و به طور خودکار بهترین حالت موجود را انتخاب می‌کند:

### 1. 📱 فایل‌های صوتی آفلاین (بهترین کیفیت)
- فایل‌های WAV شما در `res/raw/` قرار گرفته‌اند
- نام‌گذاری استاندارد انگلیسی شده‌اند
- بدون نیاز به اینترنت و با بالاترین کیفیت

### 2. 🎤 مدل هانیه (کیفیت عالی)
- مدل هوشمند فارسی برای تولید صدا
- کیفیت بسیار بالا و طبیعی
- حالت آفلاین پیشرفته

### 3. 🔊 TTS فارسی سیستم (متوسط)
- استفاده از سرویس TTS خود اندروید
- نیاز به نصب زبان فارسی در تنظیمات
- کیفیت متوسط و همیشه در دسترس

### 4. 🌐 OpenAI TTS آنلاین (هوشمند)
- استفاده از API OpenAI برای تولید صدا
- کیفیت عالی و بسیار طبیعی
- نیاز به اینترنت و کلید API معتبر

## 🚀 نحوه استفاده

### تست کامل سیستم:
```kotlin
advancedPersianTTS.testVoice()
```
این متد هر 4 حالت را تست کرده و وضعیت هر کدام را نمایش می‌دهد.

### تست هشدارهای هوشمند:
```kotlin
advancedPersianTTS.testVoiceAlert()
```
تست هشدارهای مختلف با اولویت‌های متفاوت.

### تست هشدارهای ناوبری:
```kotlin
advancedPersianTTS.testNavigationAlerts()
```
تست دستورات ناوبری با فایل‌های صوتی واقعی.

### تست هشدارهای سرعت:
```kotlin
advancedPersianTTS.testSpeedAlerts()
```
تست هشدارهای مربوط به سرعت و محدودیت‌ها.

## 🔧 تنظیمات

### فعال‌سازی حالت آنلاین OpenAI:
```kotlin
advancedPersianTTS.enableOnlineMode()
```

### غیرفعال‌سازی حالت آنلاین:
```kotlin
advancedPersianTTS.disableOnlineMode()
```

### انتخاب بین مدل هانیه و سیستم:
```kotlin
advancedPersianTTS.setTTSEngine(useSystem = false) // استفاده از هانیه
advancedPersianTTS.setTTSEngine(useSystem = true)  // استفاده از سیستم
```

## 📋 لیست فایل‌های صوتی موجود

### ناوبری:
- `turn_left.wav` - به چپ بپیچید
- `turn_right.wav` - به راست بپیچید
- `turn_left_100m.wav` - در 100 متر به چپ بپیچید
- `turn_right_100m.wav` - در 100 متر به راست بپیچید
- `turn_left_200m.wav` - در 200 متر به چپ بپیچید
- `turn_right_200m.wav` - در 200 متر به راست بپیچید
- `turn_left_500m.wav` - در 500 متر به چپ بپیچید
- `turn_right_500m.wav` - در 500 متر به راست بپیچید
- `continue_route.wav` - مسیر را ادامه دهید
- `make_u_turn.wav` - دور بزنید
- `u_turn_100m.wav` - صد متر دیگر دور بزنید
- `u_turn_300m.wav` - سیصد متر دیگر دور بزنید
- `roundabout_exit_1.wav` - در میدان از خروجی اول خارج شوید
- `roundabout_exit_2.wav` - در میدان از خروجی دوم خارج شوید
- `roundabout_exit_3.wav` - در میدان از خروجی سوم خارج شوید
- `destination_arrived.wav` - به مقصد رسیدید

### سرعت:
- `reduce_speed.wav` - سرعت خود را کاهش دهید
- `speeding_danger.wav` - خطر! سرعت غیر مجاز
- `speed_camera.wav` - دوربین کنترل سرعت
- `speed_limit_attention.wav` - توجه به محدودیت سرعت
- `speed_limit_30.wav` - محدودیت سرعت 30 کیلومتر
- `speed_limit_60.wav` - محدودیت سرعت 60 کیلومتر
- `speed_limit_80.wav` - محدودیت سرعت 80 کیلومتر
- `speed_limit_90.wav` - محدودیت سرعت 90 کیلومتر
- `speed_limit_110.wav` - محدودیت سرعت 110 کیلومتر
- `speed_limit_120.wav` - محدودیت سرعت 120 کیلومتر

### عمومی:
- `danger_ahead.wav` - احتیاط! خطر در پیش است
- `heavy_traffic.wav` - ترافیک سنگین در پیش است
- `alternative_route.wav` - مسیر جایگزین پیشنهاد می شود
- `delay_10_min.wav` - تأخیر در مسیر 10 دقیقه
- `fuel_station_1km.wav` - سوخت گیری در 1 کیلومتری
- `fuel_station_5km.wav` - سوخت گیری در 5 کیلومتری
- `parking_nearby.wav` - پارکینگ در نزدیکی شما
- `test_alert.wav` - تست هشدار صوتی فارسی

## 🎯 اولویت‌بندی سیستم

برنامه به طور خودکار به این ترتیب تصمیم می‌گیرد:
1. **فایل‌های صوتی آفلاین** (اگر موجود باشند)
2. **مدل هانیه** (اگر فعال باشد)
3. **OpenAI TTS آنلاین** (اگر اینترنت و کلید API موجود باشد)
4. **TTS فارسی سیستم** (به عنوان آخرین راه‌حل)

## 💡 نکات مهم

- برای استفاده از OpenAI TTS نیاز به کلید API معتبر دارید
- مدل هانیه کیفیت بالایی دارد اما ممکن است نیاز به تنظیمات خاصی داشته باشد
- فایل‌های صوتی آفلاین همیشه بهترین کیفیت را ارائه می‌دهند
- سیستم به طور خودکار بهترین حالت موجود را انتخاب می‌کند

## 🔍 اشکال‌زدایی

برای بررسی وضعیت سیستم:
```kotlin
advancedPersianTTS.testVoice()
```

این متد وضعیت هر 4 حالت را نمایش داده و توصیه لازم را ارائه می‌دهد.

## 📞 پشتیبانی

در صورت بروز مشکل، لاگ‌های برنامه را با تگ `AdvancedTTS` بررسی کنید. تمام مراحل اجرای سیستم در لاگ‌ها ثبت می‌شود.
