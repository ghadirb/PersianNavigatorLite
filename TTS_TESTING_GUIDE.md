# راهنمای تست کامل سیستم صوتی (TTS)

## 📋 خلاصه تغییرات

✅ **سیستم صوتی سه حالته کامل:**
- 🔊 **آفلاین:** استفاده از 43 فایل صوتی ضبط شده (.wav)
- 🌐 **آنلاین:** استفاده از OpenAI TTS API (با کلید در برنامه)
- 🤖 **خودمختار:** حالت هوشمند ترکیبی (پیش‌فرض)

## 🎯 تست قدم به قدم

### ۱. تست راه‌اندازی اولیه
1. برنامه را باز کنید
2. **باید بشنوید:** "حالت خودمختار فعال شد." + پیام خوشامدگویی
3. در Logcat ببینید: `🤖 دستیار هوشمند خودمختار از ابتدا فعال شد`

### ۲. تست انتخاب حالت‌های TTS
1. رویRadioButton ها ضربه بزنید:
   - **🔊 آفلاین:** باید بشنوید "حالت آفلاین با فایل‌های صوتی ضبط شده فعال شد"
   - **🌐 آنلاین:** باید بشنوید "حالت آنلاین با OpenAI TTS فعال شد"  
   - **🤖 خودمختار:** باید بشنوید "دستیار هوشمند خودمختار فعال شد"

### ۳. تست دکمه تست صوتی (جامع)
1. روی دکمه "تست صدا" ضربه بزنید
2. **مرحله ۱ - آفلاین:** 
   - می‌شنوید: "تست حالت آفلاین با فایل‌های صوتی"
   - می‌شنوید: فایل صوتی "تست" (test_alert.wav)
   - می‌شنوید: فایل صوتی "خطر سرعت بالا" (speeding_danger.wav)
3. **مرحله ۲ - آنلاین:**
   - می‌شنوید: "تست حالت آنلاین با OpenAI TTS" (با صدای سیستم فعلی)
4. **مرحله ۳ - خودمختار:**
   - می‌شنوید: "تست حالت خودمختار هوشمند"
   - می‌شنوید: "دستیار هوشمند فعال شد"
5. **مرحله ۴ - هشدارهای ناوبری:**
   - هشدار سرعت، مسیریابی، دوربین سرعت، رسیدن به مقصد

### ۴. تست حین رانندگی (زنده)
1. روی دکمه "شروع" ضربه بزنید
2. **باید بشنوید:**
   - "مسیریابی شروع شد. حالت آفلاین فعال."
   - فایل صوتی تست هشدار سرعت
   - "تغییر به حالت آنلاین. سیستم فعال."
   - "دستیار هوشمند خودمختار فعال شد"
   - پاسخ AI به سوال "وضعیت مسیر چطور است؟"
   - هشدار زمانی بر اساس ساعت روز
   - تحلیل وضعیت رانندگی
   - هشدار ناوبری "به چپ بپیچید"
   - هشدار دوربین سرعت

## 🔍 فایل‌های صوتی موجود (43 فایل)

### هشدارهای سرعت:
- `speeding_danger.wav` - خطر سرعت بالا
- `reduce_speed.wav` - کاهش سرعت
- `speed_limit_30.wav` تا `speed_limit_120.wav` - محدودیت سرعت

### هشدارهای ناوبری:
- `turn_left.wav`, `turn_right.wav` - پیچیدن
- `turn_left_100m.wav` تا `turn_right_500m.wav` - فاصله تا پیچ
- `roundabout_exit_1.wav` تا `roundabout_exit_3.wav` - میدان
- `alternative_route.wav` - مسیر جایگزین
- `destination_arrived.wav` - رسیدن به مقصد

### هشدارهای ایمنی:
- `speed_camera.wav` - دوربین سرعت
- `danger_ahead.wav` - خطر جلو
- `speed_bump_warning.wav` - دست‌انداز
- `sudden_stop_warning.wav` - توقف ناگهانی

### سایر:
- `test_alert.wav` - تست
- `start_navigation.wav` - شروع مسیریابی
- `heavy_traffic.wav` - ترافیک سنگین
- `fuel_station_1km.wav` - ایستگاه سوخت
- `low_fuel_warning.wav` - هشدار سوخت

## 📊 لاگ‌های مهم برای بررسی

در Logcat این پیام‌ها را جستجو کنید:

```
🤖 دستیار هوشمند خودمختار از ابتدا فعال شد
✅ حالت آفلاین با فایل‌های صوتی فعال شد
✅ حالت آنلاین با OpenAI TTS فعال شد
✅ حالت خودمختار هوشمند فعال شد
🔊 پخش فایل صوتی: [نام فایل]
🗣️ پخش با TTS سیستم: [متن]
🌐 پخش با OpenAI TTS: [متن]
🚗 شروع تست جامع AI در حین رانندگی...
✅ تست جامع AI در حین رانندگی با موفقیت انجام شد
```

## ⚠️ عیب‌یابی

### اگر فایل صوتی پخش نشد:
1. بررسی کنید فایل در `res/raw/` وجود دارد
2. در Logcat ببینید: `❌ خطا در پخش فایل صوتی`

### اگر صدایی شنیده نشد:
1. صدای گوشی را چک کنید
2. دسترسی صدا به برنامه داده شده است
3. در Logcat ببینید: `❌ خطا در راه‌اندازی TTS`

### اگر صفحه چت یا آمار باز نشد:
1. در Logcat ببینید: `❌ خطا در باز کردن صفحه`
2. بررسی کنید Activity در Manifest تعریف شده است

## 🎉 نتیجه نهایی

✅ **سه حالت TTS کاملاً فعال**
✅ **43 فایل صوتی آفلاین قابل استفاده**  
✅ **OpenAI TTS آنلاین آماده**
✅ **حالت خودمختار هوشمند پیش‌فرض**
✅ **تست جامع در دکمه تست صدا**
✅ **تست زنده در حین رانندگی**
✅ **صفحات چت و آمار کار می‌کنند**

**همه ویژگی‌ها از ابتدا فعال و کار می‌کنند!** 🚀
